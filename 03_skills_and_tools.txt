RITHUJAA RAJENDRAKUMAR - SKILLS AND TOOLS
==========================================

PROGRAMMING LANGUAGES

Python:
Proficiency: Strong — primary language, used daily
Used for: data manipulation (Pandas, NumPy), machine learning (Scikit-learn), data pipeline development, API integrations, automation scripting, LLM applications (OpenAI API, LangChain), web apps (Streamlit), PySpark
Used professionally at Sonline for ETL pipelines, dashboards, and ML models. Used at Trilemma for financial data pipelines and LLM integration. Used in all personal projects.

SQL:
Proficiency: Strong — advanced level
Used for: CTEs, window functions, stored procedures, complex multi-table joins, query optimization, dimensional modeling, star schema design, SCD1/SCD2 implementations
Used professionally at Sonline with Snowflake for client reporting and data transformation. Used in CDC pipeline with complex Snowflake stored procedures. Used in DataBridge for DuckDB star schema. Used in Spotify ETL for Snowflake table setup. SQL is present in most projects.

R:
Proficiency: Familiar — used in academic coursework and some analytical work at UBC and NYU
Used for statistical analysis, regression modeling, data visualization

Git / GitHub:
Proficiency: Comfortable — used for version control across all projects
All projects on GitHub at https://github.com/rithujaa

---

DATA ENGINEERING TOOLS

AWS S3:
Context: Used professionally at Sonline as object storage and ETL staging. Used in CDC pipeline as staging area between NiFi and Snowflake. Used in Airflow pipeline as data lake destination. Used in Spotify ETL for raw and processed data storage.

AWS Glue:
Context: Used professionally at Sonline for serverless ETL transformation and orchestration of client data pipelines.

AWS Lambda:
Context: Used in Spotify ETL pipeline — one Lambda function for extraction from Spotify API, one for transformation. Triggered automatically by S3 events.

AWS EC2:
Context: Used in CDC pipeline to host Apache NiFi in a Docker container for real-time event streaming.

Azure Data Factory (ADF):
Context: Used in E-Commerce pipeline for orchestration with event-based triggers (file arrival in ADLS triggers pipeline automatically). Used in Olympics Azure pipeline for data ingestion from GitHub to ADLS.

Azure Databricks:
Context: Used in E-Commerce pipeline for PySpark transformation across Bronze, Silver, and Gold Delta Lake layers. Used in Olympics Azure pipeline for PySpark data transformation.

Azure Synapse Analytics:
Context: Used in E-Commerce pipeline and Olympics pipeline as the final consumption layer for SQL querying and dashboarding of transformed data.

Azure Data Lake Storage Gen2 (ADLS Gen2):
Context: Used as the underlying storage in both Azure data projects (E-Commerce pipeline and Olympics pipeline) for landing zones and Delta Lake storage.

Snowflake:
Proficiency: Strong — used professionally and in depth in personal projects
Context at Sonline: Primary data warehouse for enterprise client data. Wrote advanced SQL, stored procedures, and used as the reporting layer behind Power BI dashboards.
Context in CDC pipeline: Deep Snowflake usage — Snowpipe (auto-ingest from S3), Streams (CDC tracking — detects inserts, updates, deletes), Tasks (automated stored procedure execution), SCD1 and SCD2 stored procedures, Snowflake SQL.
Context in Spotify ETL: Snowpipe for auto-loading processed data, SQL for table definitions and staging configuration.

Apache Airflow:
Context: Used in Airbnb Automated Pipeline project. Built DAGs for orchestrated data extraction from PostgreSQL to AWS S3. Developed a custom PostgresToS3Operator. Containerized with Docker.

Apache NiFi:
Context: Used in CDC pipeline for real-time event streaming from source to AWS S3. Hosted in Docker container on AWS EC2.

Apache Kafka:
Context: Used in Kafka pipeline project for real-time streaming of Citi Bikes live data to AWS S3.

Apache Spark / PySpark:
Context: Used in E-Commerce pipeline via Azure Databricks for distributed data transformation across Bronze, Silver, and Gold layers. Used in Olympics pipeline for data transformation and aggregation.

Delta Lake:
Context: Used in E-Commerce pipeline for all three Medallion Architecture layers (Bronze, Silver, Gold). Provides ACID transactions, time travel, and efficient columnar storage.

Docker:
Context: Used in CDC pipeline to containerize Apache NiFi. Used in Airflow pipeline (Docker Compose for multi-service setup).

DuckDB:
Context: Used in DataBridge as the analytical database. Designed a star schema with fact and dimension tables for cross-city restaurant inspection data.

PostgreSQL:
Context: Used in Airflow pipeline as the source database from which Airflow extracts Airbnb data.

---

MACHINE LEARNING AND DATA SCIENCE

XGBoost:
Context: Used in Airbnb ML project as the best-performing model (AUC=0.939 for classification, R2=0.652 for revenue regression). Also used in Employee Attrition project (AUC=0.79).

CatBoost:
Context: Used in Airbnb ML project as a benchmark model. Best performer in Employee Attrition project (AUC=0.82).

Random Forest:
Context: Benchmarked in Airbnb ML and Employee Attrition projects.

Logistic Regression / Linear Regression:
Context: Used as baseline models in Airbnb ML and Employee Attrition projects.

Isolation Forest:
Proficiency: Used professionally in production at Sonline
Context: Applied to wind turbine vibration sensor time-series data at Sonline. Chose Isolation Forest because the data was unlabeled (no historical failure records). Achieved 0.91 precision in detecting early-stage mechanical degradation. This work enabled a client to shift from reactive to proactive maintenance.

Survival Analysis:
Proficiency: Used professionally at Sonline
Context: Applied alongside Isolation Forest on wind turbine data to model equipment degradation over time and predict remaining useful lifespan of components.

K-Means Clustering:
Context: Used in NBA Player Similarity App to group players by statistical similarity.

HDBSCAN:
Context: Used in DataBridge for density-based clustering of restaurant violation categories across cities. HDBSCAN was chosen over K-Means because it doesn't require specifying the number of clusters in advance — useful when you don't know how many distinct violation types exist.

Feature Engineering:
Context: Extensive experience. Engineered 83 features for Airbnb ML project. Engineered 369 features across 3 data categories (on-chain metrics, macroeconomic indicators, NLP sentiment) for Trilemma Foundation strategy.

Statistical Analysis:
Context: Hypothesis testing, statistical significance testing (used to validate Trilemma backtesting results across 3,075 windows), backtesting methodology, confidence intervals.

SMOTE (Synthetic Minority Oversampling Technique):
Context: Used in Employee Attrition project to handle class imbalance in the IBM HR dataset.

---

NLP AND AI TOOLS

VADER (Valence Aware Dictionary and sEntiment Reasoner):
Context: Rule-based sentiment analysis tool. Used in Airbnb ML project to extract sentiment scores from listing description text. Used at Trilemma as a baseline sentiment signal. Fast and works well on short texts.

Sentence Transformers (all-MiniLM-L6-v2):
Context: Used in DataBridge to convert restaurant violation category text into semantic vector embeddings. These embeddings allowed HDBSCAN to group violations that meant the same thing even if worded differently.

FinBERT:
Context: A BERT-based model pre-trained specifically on financial text. Used at Trilemma Foundation to extract sentiment from financial news articles. More accurate than general-purpose models for financial language because it understands terms like "bullish", "bearish", "outperform", "underperform" in their financial context.

RoBERTa:
Context: A powerful general-purpose NLP model. Used at Trilemma for deeper text classification and understanding alongside FinBERT.

GPT-4o (OpenAI API):
Proficiency: Strong — actively building production applications with it
Context at Trilemma: Used for complex financial analysis automation — summarizing reports, extracting structured insights from unstructured text.
Context in DataBridge: Used for labeling violation taxonomy clusters and validating semantic crosswalk mappings.
Context in AI BI Assistant: Core engine — writes pandas code, generates charts, creates executive summaries, suggests questions, explains results in plain English.

GPT-4o-mini (OpenAI API):
Context: Used in Personal AI Profile Chatbot (rithujaa-ai-assistant.streamlit.app) for answer generation. 15-20x cheaper than GPT-4o, ideal for Q&A over structured documents.

Anthropic API (Claude):
Context: Familiar and has used in some automation workflows.

LangChain:
Context: Used in AI BI Assistant for the PDF RAG pipeline — specifically RecursiveCharacterTextSplitter for chunking and the retrieval chain. Also used in Personal AI Profile Chatbot.

ChromaDB:
Context: In-memory vector database used in both RAG applications (AI BI Assistant and Personal AI Profile Chatbot). Uses EphemeralClient to ensure no data bleeding between sessions.

OpenAI Embeddings (text-embedding-ada-002):
Context: Used in both RAG applications to convert text chunks into 1,536-dimensional vector embeddings for semantic similarity search.

RAG (Retrieval Augmented Generation):
Context: Implemented from scratch in two projects:
1. AI BI Assistant — RAG for PDF question answering (chunking > embedding > ChromaDB storage > retrieval > GPT-4o generation)
2. Personal AI Profile Chatbot — RAG over 6 structured profile documents (same pipeline, GPT-4o-mini for generation)

Prompt Engineering:
Context: Used extensively across all LLM projects. Examples include: telling GPT-4o to use only specific Python libraries (pandas, numpy, scipy) and never matplotlib; instructing GPT-4o to return only JSON with no markdown; engineering prompts that make GPT-4o reliably produce structured outputs.

n8n (Workflow Automation):
Currently learning. n8n is an open-source workflow automation tool for building agentic AI pipelines. Exploring how to connect LLMs to data sources, APIs, and automated workflows without heavy coding.

Agentic AI:
Understanding and building agentic systems where AI takes autonomous actions to complete multi-step tasks. The AI BI Assistant includes a simple agentic loop — write code > execute > catch errors > retry with different approach.

---

BUSINESS INTELLIGENCE AND VISUALIZATION

Power BI:
Proficiency: Strong — used professionally in production
Context: Built dashboards for 3+ enterprise clients at Sonline. Connected to Snowflake as data source. Created KPI scorecards, trend charts, and operational monitoring dashboards used daily by client teams.

Tableau:
Proficiency: Strong — built and deployed live dashboard
Context: Built Amazon Stock Performance Dashboard (live at https://public.tableau.com/app/profile/rithujaa.rajendrakumar4680/viz/Rajendrakumar_Rithujaa_assignment_2/Dashboard1) with KPI scorecards, trend visualizations, and dynamic date-range parameters.

Streamlit:
Proficiency: Strong — built and deployed multiple production applications
Applications built: AI BI Assistant (ai-bi-assistant.streamlit.app), Personal AI Profile Chatbot (rithujaa-ai-assistant.streamlit.app), Trilemma strategy monitoring dashboard, NBA Player Similarity App (nba-player-similarity-tool.streamlit.app).

Plotly Express:
Context: Used in AI BI Assistant for automatic interactive chart generation. GPT-4o writes the Plotly code; the app renders it.

Microsoft Excel:
Context: PivotTables, advanced formulas, Power Query for data validation and ad hoc analysis.

---

DATA QUALITY AND GOVERNANCE

Data Validation Frameworks:
Context: Built Python reconciliation scripts in CDC pipeline to verify record counts between RAW, SCD1, and SCD2 layers. Used precision and recall metrics to validate DataBridge crosswalk quality.

Documentation Practices:
Context: Maintains pipeline architecture documentation, data lineage records, and transformation logic for all major projects.

SCD1/SCD2 Dimensional Modeling:
Context: Implemented in CDC pipeline. SCD1 uses MERGE for current-state upserts. SCD2 tracks full history with effective dates (START_DATE, END_DATE, CURRENT_FLAG).

Star Schema Design:
Context: Designed and implemented in DataBridge (DuckDB) and CDC pipeline (Snowflake). Includes fact and dimension tables with proper primary and foreign key relationships.

---

PROFICIENCY SUMMARY
- Python: Strong
- SQL: Strong
- Power BI: Strong
- Tableau: Strong
- Snowflake: Strong (professional + deep project experience)
- Machine Learning: Strong (0.91 precision Isolation Forest at Sonline, XGBoost AUC=0.939, 9 models benchmarked, survival analysis in production)
- LLMs / GPT-4o: Strong (building production AI applications)
- RAG: Intermediate-Strong (implemented in 2 deployed applications)
- AWS: Intermediate-Strong (professional use at Sonline + S3, Glue, Lambda, EC2 in projects)
- Azure: Intermediate-Strong (ADF, Databricks, Synapse, ADLS Gen2 in projects)
- PySpark: Intermediate (E-Commerce and Olympics pipelines via Databricks)
- Apache Airflow: Intermediate (full pipeline with custom operator)
- LangChain: Intermediate (RAG pipeline implementation)
- dbt: Basic familiarity
- Looker: Not currently in stack
- n8n: Currently learning
